# TBXDialectSchemaGenerator


TBX Dialect Schema Generator is a tool which will generate a Schematron schema to enforce the constraints of one or more TBX modules (which together constitute a TBX dialect).
